# Development default uses the Vite proxy.
# Leave this unset unless you deploy frontend separately.
# VITE_API_BASE_URL=/api/v1

# Frontend resiliency settings for high-traffic use
VITE_REQUEST_TIMEOUT_MS=15000
VITE_PRODUCT_CACHE_TTL_MS=60000
